# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# JDK 路径（Android Studio 自带的 JBR）
org.gradle.java.home=F:/ProgramFile/Android Studio/jbr

# Use AndroidX
android.useAndroidX=true

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enable parallel execution
org.gradle.parallel=true

# Enable build cache
org.gradle.caching=true

# 网络配置 - 解决连接Google服务器问题
# 如果使用代理，取消注释并配置以下行：
# systemProp.http.proxyHost=127.0.0.1
# systemProp.http.proxyPort=7890
# systemProp.https.proxyHost=127.0.0.1
# systemProp.https.proxyPort=7890

# 跳过SSL证书验证（不推荐，但可临时解决问题）
# systemProp.javax.net.ssl.trustStore=NONE
# systemProp.javax.net.ssl.trustStoreType=Windows-ROOT

# 增加网络超时时间
systemProp.http.connectionTimeout=30000
systemProp.http.socketTimeout=60000
systemProp.https.connectionTimeout=30000
systemProp.https.socketTimeout=60000

# 禁用Gradle守护进程（有时可以解决连接问题）
# org.gradle.daemon=false

# 强制使用IPv4（某些网络环境下IPv6可能有问题）
java.net.preferIPv4Stack=true
